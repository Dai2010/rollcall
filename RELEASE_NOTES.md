# 点名助手 v0.0.3

## 新增功能

- 程序每次启动后会在后台自动检查最新版本，不阻塞主窗口。
- 新增“检查更新”页面，显示当前版本、最新版本、检查状态、发布时间与更新说明。
- 支持手动重新检查，并可直接打开 GitHub Release 下载页面。
- Windows 用户可通过 `ghfast.top` 代理下载新版安装包；下载完成后自动启动安装程序并退出旧版本。

## 稳定性

- 断网、请求超时、GitHub API 限流或返回异常时会在更新页面给出提示，不影响点名功能。
- 使用 GitHub 公共 Releases API 查询正式版本，不需要登录或提供访问令牌。
- 代理下载的安装包必须通过 GitHub Release 提供的文件大小和 SHA-256 双重校验，否则不会启动。
